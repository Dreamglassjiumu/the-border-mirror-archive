import type { Plotline } from '../data';
export function PlotlineTimeline({ plotlines, onSelect }: { plotlines: Plotline[]; onSelect: (plotline: Plotline) => void }) {
  return <div className="relative space-y-5 before:absolute before:left-5 before:top-4 before:h-[calc(100%-2rem)] before:w-px before:bg-gradient-to-b before:from-cyan-100/20 before:via-stardust/30 before:to-transparent">{plotlines.map((line, index) => <button key={line.id} onClick={() => onSelect(line)} className="relative ml-12 w-[calc(100%-3rem)] rounded-3xl border border-white/10 bg-white/[0.055] p-5 text-left hover:border-stardust/50"><span className="absolute -left-[2.65rem] top-6 grid h-8 w-8 place-items-center rounded-full border border-stardust/30 bg-[#080b18] text-xs text-stardust">{index + 1}</span><p className="text-xs uppercase tracking-[0.3em] text-cyan-100/45">{line.type}</p><h3 className="mt-2 text-xl font-semibold text-white">{line.title}</h3><div className="mt-4 grid gap-3 text-sm text-slate-300 md:grid-cols-3"><p><b className="text-stardust/80">Start</b><br />{line.start}</p><p><b className="text-stardust/80">Midpoint</b><br />{line.midpoint}</p><p><b className="text-stardust/80">End</b><br />{line.end}</p></div></button>)}</div>;
}
