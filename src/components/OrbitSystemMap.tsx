import type { WorldSystem } from '../data';
export function OrbitSystemMap({ systems, onSelect }: { systems: WorldSystem[]; onSelect?: (system: WorldSystem) => void }) {
  return <section className="relative mx-auto my-10 grid min-h-[520px] max-w-5xl place-items-center rounded-[2rem] border border-white/10 bg-black/20 p-6 overflow-hidden"><div className="absolute h-72 w-72 rounded-full border border-stardust/25 shadow-[0_0_80px_rgba(218,188,111,.18)]" /><div className="absolute h-[28rem] w-[28rem] rounded-full border border-cyan-100/10" /><div className="z-10 rounded-full border border-stardust/30 bg-[#080b18]/80 px-8 py-6 text-center"><p className="text-xs uppercase tracking-[0.35em] text-stardust">Archive Core</p><h2 className="mt-2 text-2xl font-semibold text-white">Seven World Systems</h2></div>{systems.map((system, index) => { const angle = (index / systems.length) * Math.PI * 2 - Math.PI / 2; const x = Math.cos(angle) * 39 + 50; const y = Math.sin(angle) * 39 + 50; return <button key={system.id} onClick={() => onSelect?.(system)} style={{ left: `${x}%`, top: `${y}%` }} className="group absolute w-36 -translate-x-1/2 -translate-y-1/2 rounded-2xl border border-cyan-100/20 bg-[#0b1020]/85 p-3 text-left shadow-xl backdrop-blur transition hover:border-stardust/60 hover:bg-stardust/10"><span className="text-[10px] uppercase tracking-[0.25em] text-cyan-100/45">{system.englishName}</span><strong className="mt-1 block text-sm text-white">{system.title}</strong><span className="mt-2 hidden text-xs leading-5 text-slate-300 group-hover:block">{system.summary} · {system.entryCount} entries</span></button>; })}</section>;
}
